os: linux

language: go

go: "1.14"

notifications:
  email: false

services:
  - docker

jobs:
  include:
    - stage: test
      script: docker-compose up --build

script:
  - cd $GOPATH/src/github.com/VictoryKS/design-practice-3/integration
  - go test -bench=.
